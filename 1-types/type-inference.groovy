
// infer type to a, according with assign value
a  = 1

// interpolation of string only for '"' strings
println "the class of a is ${a.class.name}"

// infer type to a, according with assign value
a += 1000000000000000000000

// interpolation of string only for '"' strings
println "the class of a is ${a.class.name}"
